Run the local privacy filter's status command and explain the result:

```
shade status
```

Summarise for me: whether shade is enabled, the policy in force for each
surface (prompt, egress, shell, local_write, local_read, output), how many names
are configured, and how many entries the vault holds. Flag any surface set to
`off` and say what that means in practice. Do not invent settings that are not
in the output.
